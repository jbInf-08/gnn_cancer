# Archived drafts and long-form writeups

This directory holds **non-canonical** narrative documents (old reports, planning
notes, and similar). They are kept for project history; **do not** cite them as
reproducible results without tracing claims back to code and your own runs.

The canonical project description is the root `README.md`. A minimal results
template lives in `notebooks/results_summary.ipynb`.
