# Editable install is recommended (pulls this list from pyproject):
#   pip install -e ".[dev]"
#
# If you need a flat requirements file without PEP 621, use pyproject.toml as the
# source of truth, or:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install torch-geometric "torch-scatter" "torch-sparse"  # + PyG index if needed
#   pip install pandas numpy scikit-learn networkx matplotlib seaborn tqdm requests
#   pip install transformers wandb imbalanced-learn umap-learn shap lime xgboost biopython

torch>=2.0
torch-geometric>=2.3
pandas>=1.5
numpy>=1.24
scikit-learn>=1.2
networkx>=3.0
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65
requests>=2.31
transformers>=4.30
wandb>=0.15
imbalanced-learn>=0.11
umap-learn>=0.5
shap>=0.42
lime>=0.2
xgboost>=1.7
biopython>=1.81
pytest>=7.4
